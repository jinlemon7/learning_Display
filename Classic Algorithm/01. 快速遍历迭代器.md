实现了Iterable接口的类/数组可以使用for-in语法快速遍历。  
- 对于Iterable 对象：编译成使用 iterator() 方法的循环。
- 对数组：编译成普通的 for 循环，基于索引遍历。

例：  
```java
ArrayList<Integer> a = new ArrayList<>(Arrays.asList(1, 2, 3) );

for( var t : a )
  System.out.println(t);
```
